
var mysql = require('mysql');
var utils = {};

utils.connect = function connect() {
    var connection = mysql.createConnection({
        host     : 'localhost',
        user     : 'CLI1SQLUSR',
        password : 'xxx321',
        database : 'cli1'
    });
    connection.connect(function(err, connection) {
        if (err) {
            console.error('CONNECTION error: ',err);
            res.statusCode = 503;
            res.send({
                result: 'error',
                err:    err.code
            });
        }
    });
    return connection;
}

module.exports = utils;